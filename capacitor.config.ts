import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.angularstarter.app',
  appName: 'Angular Starter',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
